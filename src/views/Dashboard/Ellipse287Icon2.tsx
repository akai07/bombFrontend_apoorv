import { memo, SVGProps } from 'react';
import React, {useCallback, useMemo} from 'react';

const Ellipse287Icon2 = (props: SVGProps<SVGSVGElement>) => (
  <svg viewBox='0 0 25 25' fill='none' xmlns='http://www.w3.org/2000/svg' {...props}>
    <circle cx={12.5} cy={12.5} r={12.5} fill='#373747' />
  </svg>
);

const Memo = memo(Ellipse287Icon2);
export { Memo as Ellipse287Icon2 };
